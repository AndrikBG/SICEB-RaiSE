/**
 * Branch Management module (Platform) — Branch CRUD, active branch selection,
 * {@code branch_id} injection into request context for tenant-scoped queries.
 *
 * <p>Implementation: Phase 1 (schema), Phase 3 (full context injection).
 */
package com.siceb.platform.branch;
