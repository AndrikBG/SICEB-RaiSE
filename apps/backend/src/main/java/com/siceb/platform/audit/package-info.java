/**
 * Audit and Compliance module (Platform) — Centralized append-only audit log.
 * Write-only sink: domain modules push events, Audit never calls back.
 *
 * <p>Implementation: Phase 3 (Security).
 */
package com.siceb.platform.audit;
