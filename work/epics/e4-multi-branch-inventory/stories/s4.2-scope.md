# Scope — S4.2: Inventory CQRS + Real-Time Propagation

## In Scope

- Testcontainers integration test infrastructure (gap G2)
- Flyway migrations: `inventory_items` + `inventory_deltas` tables (partitioned by `branch_id`)
- StockMaterializationTrigger: PG `AFTER INSERT` trigger + `pg_notify`
- InventoryCommandHandler: 5 delta types (increment, decrement, adjust, threshold, expiration)
- InventoryDeltaStore: append-only, idempotent by key
- BranchInventoryReadModel: admin cross-branch + service-scoped views
- LowStockAlertProjection + ExpirationAlertProjection
- RealtimeEventPublisher: `pg_notify` → STOMP bridge
- WebSocketSecurityInterceptor: JWT auth on CONNECT + subscription authorization
- PWA InventoryRealtimeManager: WebSocket subscription lifecycle
- RLS policies on `inventory_items` and `inventory_deltas`
- admin_reporting grants on new tables
- Permission seed: `inventory:read_all`, `inventory:read_service`, `inventory:adjust`
- REST: delta commands, inventory queries, search, export

## Out of Scope

- PWA inventory views (S4.4 — InventoryDashboardView, ServiceInventoryView)
- Tariff tables and service (S4.3)
- Offline sync replay (Phase 6)
- Supply request workflow (Phase 5)
- Pharmacy dispensation stock deduction (Phase 5)
- Horizontal WebSocket scaling (single-instance sufficient)
- React Router routes for /inventory (S4.4)

## Done Criteria

- [ ] Testcontainers runs PG integration tests with triggers, RLS, and partitioning
- [ ] 5 delta command types accepted via REST with idempotency enforcement
- [ ] StockMaterializationTrigger materializes stock within same transaction
- [ ] pg_notify fires on delta insert with payload < 8KB
- [ ] STOMP push delivers inventory change to subscribed client < 2 seconds end-to-end
- [ ] WebSocket JWT auth rejects unauthorized CONNECT and SUBSCRIBE
- [ ] RLS enforced: service manager sees only own service's inventory
- [ ] Admin cross-branch view uses admin_reporting bypass
- [ ] Partition pruning confirmed via EXPLAIN ANALYZE
- [ ] LowStockAlert flags items correctly (OK / LOW_STOCK / OUT_OF_STOCK)
- [ ] ExpirationAlert categorizes items (OK / EXPIRING_SOON / EXPIRED)
- [ ] PWA InventoryRealtimeManager subscribes, receives, reconnects
- [ ] All tests pass (unit + integration), type checks clean, lint clean

## Dependencies

- **S4.1** (done): branch registration, onboarding orchestrator, context switching, RLS infra
